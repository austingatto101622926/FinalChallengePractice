@model IEnumerable<FinalChallengePractice.Models.CoffeeDate>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Venue)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DateTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Venue)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CoffeeDateId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CoffeeDateId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CoffeeDateId })
        </td>
    </tr>
}

</table>
